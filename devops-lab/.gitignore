# --- AWS & SSH Secrets ---
*.pem
*.key
.aws/
credentials

# --- Terraform Files ---
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
*.tfvars
# Note: Keep .tfvars if they only contain non-sensitive defaults, 
# but ignore them if they contain secrets.
!example.tfvars

# --- Ansible Files ---
*.retry
ansible_fact_cache/

# --- Operating System Files ---
.DS_Store
Thumbs.db
desktop.ini

# --- IDE Files ---
.vscode/
.idea/
*.swp

# Ignore the terraform binary
terraform.exe

# Ignore the terraform working directory (where providers are kept)
.terraform/
**/.terraform/*

.terraform/
terraform.exe
*.tfstate
*.tfstate.backup
*.pem